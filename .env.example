# Required: Your Cloudinary cloud name
CLOUDINARY_CLOUD_NAME=your_cloud_name

# Required: Your Cloudinary API key (needed for both signed and unsigned uploads)
CLOUDINARY_API_KEY=your_api_key

# Option 1: Signed uploads (requires API secret - more secure, but secret must be kept private)
CLOUDINARY_API_SECRET=your_api_secret

# Option 2: Unsigned uploads (for sharing with others - requires upload preset)
# If using unsigned uploads, create an upload preset in Cloudinary dashboard
# and set it here. You still need CLOUDINARY_API_KEY above.
# CLOUDINARY_UPLOAD_PRESET=your_upload_preset_name

PORT=3000

# Canva app CORS origin — set after creating your app in the Developer Portal
# Format: https://app-<APP_ID>.canva-apps.com
ALLOWED_ORIGIN=https://app-YOUR_APP_ID.canva-apps.com

# Backend URL baked into the frontend at build time.
# Set as a shell env var before running `npm run build`, or add it here.
# CANVA_BACKEND_HOST=https://your-app.up.railway.app
